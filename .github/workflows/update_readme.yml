name: Update README with Latest Repos

on:
  schedule:
    # Menjalankan setiap 6 jam
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update README with latest repositories
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          # URL feed dari repositori publik Anda
          feed_list: "https://api.github.com/users/Kyugito666/repos?sort=created&direction=desc"
          # Jumlah maksimum repositori yang akan ditampilkan
          max_post_count: 5
          # Path ke file README.md Anda
          readme_path: 'README.md'
          # Tag komentar di README tempat daftar akan disisipkan
          comment_tag_name: "LATEST_REPOS"
          # Template untuk setiap item daftar
          template: "- **[$title]($url)**: $description"
